<?xml version="1.0"?>
<block>
  <name>comparator_py_f</name>
  <key>module1_comparator_py_f</key>
  <category>[module1]</category>
  <import>import module1</import>
  <make>module1.comparator_py_f($value)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Value</name>
    <key>value</key>
    <type>float</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>float</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <!--<source>
    <name>out</name>
    <type>float</type>
  </source>-->
</block>
